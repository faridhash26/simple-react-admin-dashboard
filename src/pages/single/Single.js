import React from "react";
import styled from "styled-components";

const Single = () => {
  return <SingleWrapper>Single</SingleWrapper>;
};
const SingleWrapper = styled.div(() => {
  return {};
});
export default Single;
